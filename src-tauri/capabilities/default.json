{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：允许前端调用核心 API（含自定义命令 get_backend_port）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
